[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --ignore=tests/test_health.py
markers =
    integration: tests that require external services (db, redis, ollama)
    unit: fast tests with no external dependencies
